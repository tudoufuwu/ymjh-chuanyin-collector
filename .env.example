# Keep this file value-free. Copy it to .env only if a deployment wrapper needs it.
# The default local collector does not require credentials.
#
# Optional deployment integrations must be configured outside Git:
# YMJH_PUBLIC_BASE_URL=
# YMJH_TUNNEL_NAME=
