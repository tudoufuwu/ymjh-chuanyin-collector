# 贡献指南

感谢你改进一梦江湖传音采集。

1. 提交前不要加入真实 JSONL、SQLite、截图、日志、抓包、账号、会话或密钥。
2. 采集器仅应保持只读：不得向目标进程写内存、注入代码、修改权限或绕过认证。
3. 为行为改动提供合成测试夹具；不要用真实玩家资料做测试。
4. 保持 Python 标准库优先，新增第三方依赖时说明理由、许可证和安装方式。
5. 更新 README 或 `docs/`，使启动、备份、恢复和隐私说明保持一致。

本项目以 GPL-3.0 发布；提交的代码须可在该许可证下再发布。
