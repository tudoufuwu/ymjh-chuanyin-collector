# 安全政策

## 支持范围

本项目默认只提供本地监听的归档网页，以及只读的 Windows 进程读取能力。它不处理账号密码、网络会话、隧道凭据或远程控制。

## 报告问题

请勿在公开 Issue 中发布真实聊天、数据库、进程内存、截图、会话标识或凭据。对可能泄露这些信息的问题，请使用仓库维护者在 GitHub 个人资料中公布的私密联系方式；若没有私密联系方式，请先创建最小化、脱敏的公开说明。

## 部署提示

默认服务地址是 `127.0.0.1:8766`。如自行暴露到公网，请先在反向代理或 Tunnel 层配置认证、访问控制和 HTTPS，且不要把任何 Token、域名、Tunnel ID 或密钥提交到仓库。
